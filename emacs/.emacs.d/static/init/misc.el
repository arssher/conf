;; whatever doesn't seem to deserve its own file

;; don't create lockfiles provoking warnings like
;; "it has been detected that you started modifying file someone else is also editing"
;; or something
(setq create-lockfiles nil)
